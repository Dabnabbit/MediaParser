# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must always use LF (CRLF breaks #!/bin/bash in Docker)
*.sh text eol=lf

# Windows batch files use CRLF (required by CMD.exe)
*.bat text eol=crlf
